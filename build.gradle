buildscript {
    repositories {
        google()  // Added this
        mavenCentral()  // Replaced jcenter
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.1'  // Updated version
        // Removed old plugins
    }
}

allprojects {
    repositories {
        google()  // Added this
        mavenCentral()  // Replaced jcenter
    }
}
